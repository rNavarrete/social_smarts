class API::MentionsController

def index
  @mentions = current_user.fetch_mentions
  @mentions.as_json
end

def show
  @mentions = current_user.fetch_mentions
  mention = @mentions.detect { |mention| mention.id == params[:id] }
  respond_with mention
end

routes:

namespace api
  resources :mentions
  resources :mentions
end
end

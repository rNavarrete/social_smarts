<h1>Tweets</h1>
<% if @tweets %>
<%# if @tweets.empty? %>
<ul class="tweets">
  <% @tweets.each do |tweet| %>
  <li class="tweet"><%= tweet.text %></li>
  <% end %>
</ul>
<% else %>
<h1>You have no tweets</h1>
<% end %>
